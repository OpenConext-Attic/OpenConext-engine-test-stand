parameters:
    locale:            en
    secret:            ThisTokenIsNotSoSecretChangeIt

    database_driver:   pdo_mysql
    database_host:     127.0.0.1
    database_port:     ~
    database_name:     symfony
    database_user:     root
    database_password: ~

    database_accounts_driver:   pdo_mysql
    database_accounts_host:     127.0.0.1
    database_accounts_port:     ~
    database_accounts_name:     whatever
    database_accounts_user:     whatever
    database_accounts_password: blablabla

    mailer_transport:  smtp
    mailer_host:       127.0.0.1
    mailer_user:       ~
    mailer_password:   ~

    router.request_context.host: engine-test-stand.demo.openconext.org
    router.request_context.scheme: https

    engineblock_url:        https://engine-test.demo.openconext.org

    # Where can we find the JANUS JSON export for IDPs and SPs?
    idps_config_url:        /tmp/saml20-sp-remote.json
    sps_config_url:        /tmp/saml20-sp-remote.json

    # Where must we store the writable state of the Mock IdP and Mock SP?
    idp_fixture_file:     fixtures/db/idp.states.php.serialized
    sp_fixture_file:      fixtures/db/sp.states.php.serialized
